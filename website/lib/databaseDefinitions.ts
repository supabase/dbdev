export interface definitions {
  packages: {
    id: string
    slug: string
    name: string
    package_name: string
    owner: string
    handle: string
  }
  package_versions: {
    id: string
    version: string
    object_id: string
    object_key: string
  }
}
